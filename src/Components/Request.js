import React from "react";
import "./Request.css";

export default function request({ title }) {
    return (        
        <div className="episodeTile">
          {title}
        </div>
        
    );
}

